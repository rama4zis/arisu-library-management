server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/{yourDB}
    username: 
    password: 
    hikari:
      minimum-idle: 10
      maximum-pool-size: 15
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        enable_lazy_load_no_trans: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  profiles:
    active: development
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

app:
  upload-directory: library-uploads
  folder-temp: ./library-temp/

logging:
  level:
    com.arisu_library.arisu_library_management: TRACE

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    defaultModelsExpandDepth: -1
    docExpansion: none
    tagsSorter: alpha
    operationsSorter: alpha
